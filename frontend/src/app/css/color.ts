/** Moon Traders カラーパレット定数 */
export const COLOR = {
  background: 'var(--background)',
  card: 'var(--card)',
  border: 'var(--border)',
  foreground: 'var(--foreground)',
  primary: 'var(--primary)',
} as const;
